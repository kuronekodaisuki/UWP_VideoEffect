<Page
    x:Class="VideoEffectApp.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:VideoEffectApp"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <StackPanel Orientation="Vertical" Margin="10" Grid.Row="0" VerticalAlignment="Top" Height="30">
            <StackPanel Orientation="Horizontal">
                <Button x:Name="buttonCapture" Content="Capture" Click="ButtonCapture_Click" Margin="0,0,5,0"/>
            </StackPanel>
         </StackPanel>
        <MediaElement x:Name="videoPlayer" Grid.Row="1" Stretch="Uniform" RealTimePlayback="True" AreTransportControlsEnabled="True" />
    </Grid>
</Page>
